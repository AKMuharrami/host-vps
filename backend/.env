# Backend Configuration
PORT=3005
NODE_ENV=production

# Token for Vercel Blob (Optional, used for temporary video storage if enabled)
BLOB_READ_WRITE_TOKEN=

# System paths
SYSTEM_FFMPEG_PATH=ffmpeg

# Performance settings (Optimized for small VPS)
GPU_COUNT=0
MAX_CONCURRENT_JOBS=1
